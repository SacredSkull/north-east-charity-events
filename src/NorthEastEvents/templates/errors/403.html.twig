{% extends "/errors/error.html.twig" %}
        {% block body %}
        <h1 >{{resource_type}}</h1>
        <p>{% if current_user %}{{ unauthorised_message }}{% else %}You need to login to modify this {{resource_type}}.{% endif %}</p>
        {% endblock body %}
