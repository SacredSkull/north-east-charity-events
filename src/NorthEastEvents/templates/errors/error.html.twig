{% extends "skeleton.twig.html" %}
        {% block body %}
        <h1 >Something went wrong with that {{resource_type|default("request")}}.</h1>
        <p>{{error_message}}</p>
        {% endblock body %}
